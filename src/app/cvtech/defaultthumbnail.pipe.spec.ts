import { DefaultthumbnailPipe } from './defaultthumbnail.pipe';

describe('DefaultthumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultthumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
